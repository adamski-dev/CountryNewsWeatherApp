// The 'News' interface representing a news object with key attributes used by application
export interface News {
    image_url: string;
    title: string;
    link: string;
    description: string;
}